const val = 5;
const myName = "Zakir";
console.log("TypeScript : " + val + " " + myName)

const person: number[] = [10,22,55];

type Student = {name: string, age: number};

const student: Student = {name: "Zakir", age: 40}